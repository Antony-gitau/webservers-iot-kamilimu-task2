const debug = require('debug')('antony');

debug("variable value... through debug");

//
console.log("varible value ... through console");

let arr = [].fill(123, 0, 10000) //create an array with 10000 stuff
let arr2 = arr.map(item => item *10); //going through the loop
debug('after the loop');